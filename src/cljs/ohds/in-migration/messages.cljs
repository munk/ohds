(ns ohds.in-migration.messages)

(defrecord MigrationDate [date])
(defrecord Origin [origin])
(defrecord Reason [reason])
(defrecord Type [type])
(defrecord Submit [])

(defrecord ImplementMe [])
(defrecord ImplementMeResponse [body])
